# advanced features

- a note may not appear multiple times in the same hierarchy
- note in the domain
- unrelated tags in the same maincell


define how hierarchy works


define how batch data operation works

- hit `<delete>` on a focused subcell
  - remove tag from the focused subcell
  - the rest of the occupied subcell are push forward by one cell unit
for a node in a hierarchy
what would happened afterward?
- the children is removed from the parent
- the tag persist however, the relation break
- the tag-note mappings persist
- check if there's name confliction after the break
- if confliction exists, suffix the incoming one with underscore