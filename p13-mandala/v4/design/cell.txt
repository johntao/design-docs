please help make a blank HTML with the following features:

## View

- a main wrapper in the out most layer
- inside the main is an editable record (the master record)
- the first row of the main contains an editable title
- the second row of the main contains an editable description

## Interaction

### mouse


### keyboard

- user hit `i` to create or update the master record
  - title and description are now editable (i.e. enter edit mode)
  - hit `<esc>` to discard the changes
  - hit `<enter>` to apply the changes
    - title is required
    - if the title is blank or whitespace, does nothing, notify users for violation
    - if the title is not blank, apply changes, exit edit mode
- user hit `<del>` to delete the master record
  - notify deletion with an undo button

---

add the following features:

- user click on the record would make the element focused
- a focused element will be highlighted by extra outlines
- shortcut `i` and `<del>` only works on a focused element

---

add the following features:

- replace the second row of the main by a list of detail records
  - each list item expose the title of a detail record
  - click on the record to open an update modal popup
  - the list display 5 items at max
- hit `u` on a focused element
  - open a creation modal popup to create a detail record
- hit `n` on a focused element
  - pop the last detail records, then, unshift it back to the list; repeat 3 times
- hit `m` on a focused element
  - shift the first detail records, then, push it back to the list; repeat 3 times
- update and creation modal popup
  - containing a required title field and an optional description field
  - hit `<esc>` to cancel the operation, and close the modal
  - hit `<enter>` to confirm the changes
    - if the title is blank or whitespace, does nothing, notify users for violation
    - if the title is not blank, create the record, close the modal
- hit `<del>` on a focused element now also delete all the detail records

---

we need to rework all the focus and actions:

first of all, add another dummy focusable element next to `<main>` such that we can validate our newly introduce focus logic

treat `<main>` as a focusable element where it may contains zero to one master record

the initial state should be an empty `<main>` instead of containing a blank master record

there are two types of focus: inner focus and outer focus
- inner focus are triggered when user trying to click on the title field of a master record or click on a detail record
- outer focus are triggered when the user click on "main"

when an outer focus is triggered:
- a default inner focus is triggered
- the inner focus defaults to the title of a master record
- the outer element remains the highlighting effect

when an inner focus is triggered:
- highlight the outer element as well

when an outer focus exists:
- hit `u` will open a creation modal popup:
  - if the main is empty, then, the modal create a master record
  - if the main is occupied, then, the modal create a detail record of a master record
- hit `n` pop the last detail records, then, unshift it back to the list; repeat 2 times
- hit `m` shift the first detail records, then, push it back to the list; repeat 3 times

when an inner focus exists:
- hit `i` will edit the title of the focused element
- hit `o` will open an update modal popup
- hit `<del>` will delete the focused element

a master record update modal now contains three fields:
- (required)title
- description
- list all the detail records
  - expose the title field
  - user may drag and drop to reorder the detail records

---

I found out that open-close an update modal for a detail record will lose the previous inner focus on the selected detail record

actual: after closing the modal, the default inner focus is triggered

expected: after closing the modal, resume the previous inner focus 

please help address this issue

---

lastly, let's introduce a few more shortcut keys for inner focus:
- hit arrow keys up/ down should move the inner focus up and down between title of the master record and detail records